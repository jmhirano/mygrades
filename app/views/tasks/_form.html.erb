<%= form_for([@category,@task]) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<p>
  <b>Course:</b>
  <%= Course.find(@category.course_id).name%>
</p>

<p>
  <b>Category:</b>
  <%= @category.name %>
</p>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %><br />
  </div>
  <div class="field">
    <%= f.label :total %>
    <%= f.number_field :total %><br />
  </div>
  <div class="field">
    <%= f.label :due_date %>
    <%= f.datetime_select :due_date %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
